# docker-compose.yml
services:
  fragments-backend:
    image: fragments-backend:latest
    init: true
    ports:
      - '80:8080'
    environment:
      - NODE_ENV=production
      - HTPASSWD_FILE=tests/.htpasswd
